# dummy_tool_python

This is the tool a trial to understand integrating a new tool into the framework, echos back a message.

## Features

- echo back a message given by the user

## API

Refer to `api` definition in [manifest.json] and default values in [property.json](property.json).

### Out:

- `tool_register`: auto register tool to llm

### In:

- `tool_call`: sync cmd to fetch weather
